"use strict";

var getActions = require("./getActions");

var performAction = require("./performAction");

var salute = require("./salute");

module.exports = {
  getActions: getActions,
  performAction: performAction,
  salute: salute
};